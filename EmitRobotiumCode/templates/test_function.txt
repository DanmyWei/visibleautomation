	public void testRun() throws TestException, IOException, ClassNotFoundException {
		// to provide access to asset files for motion events and stuff like that.
	    String testClassName = this.getClass().getSimpleName();